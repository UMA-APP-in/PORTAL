{
  "name": "uma-proxy",
  "version": "1.0.0",
  "description": "UMA proxy + student portal backend",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "validator": "node -e \"const fs=require('fs'); const path=require('path'); const os=require('os'); const isWin = os.platform()==='win32'; const venvPath = isWin ? path.join('photo', '.venv', 'Scripts', 'python.exe') : path.join('photo', '.venv', 'bin', 'python'); const cmd = fs.existsSync(venvPath) ? venvPath : (isWin ? 'python' : 'python3'); console.log('Starting validator with:', cmd); require('child_process').spawn(cmd, ['-m', 'uvicorn', 'photo.validator_api:app', '--host', '127.0.0.1', '--port', '8000'], {stdio: 'inherit', shell: true})\""
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "axios": "^1.13.2",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-session": "^1.18.0",
    "form-data": "^4.0.5",
    "jszip": "^3.10.1",
    "multer": "^2.0.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  },
  "engines": {
    "node": ">=18"
  }
}